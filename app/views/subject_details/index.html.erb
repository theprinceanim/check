<p id="notice"><%= notice %></p>

<div class="container">
  <div class="row">

<!-- <div class="col-xs-2">
<%= link_to '<i class="glyphicon, glyphicon-plus"></i> New Subject details'.html_safe, students_path, class: 'btn btn-default btn-lg' %>

    
  </div> -->

   
    <div class="col-xs-10">
    <div class="container">
    
        <p><strong>Name: <%= @student.last_name %>
  <%= @student.first_name %></strong></p>
     <p><strong>Exams Number: <%= @student.exams_number %></strong></p>
    </div>
  
    <center>
     

 
<table class="rwd-table">

    <tr>
      <th>Subject code</th>
      <th>Subject name</th>
      <th>Mark</th>
      <th>Grade</th>
         
      <th colspan="3"></th>
    </tr>
  

  <tbody>
    <% @subject_details.each do |subject_detail| %>
      <tr>
        <td><%= subject_detail.subject_code%></td>
        <td><%= subject_detail.subject_name %></td>
        <td><%= subject_detail.mark %></td>
        <td><%= subject_detail.grade %></td>
      
        <td><%= link_to 'Show', student_subject_detail_path(@student, subject_detail) %></td>
        <td><%= link_to 'Edit', edit_student_subject_detail_path(@student, subject_detail) %></td>
        <td><%= link_to 'Destroy', student_subject_detail_path(@student, subject_detail), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</center>




  <div class="col-xs-2">
<%= link_to '<i class="glyphicon, glyphicon-plus"></i> New Subject details'.html_safe, new_student_subject_detail_path(@student), class: 'btn btn-default btn-lg' %>

<%= link_to '<< Back'.html_safe, url_for(:back), class: 'btn btn-default btn-lg' %>
<%#= link_to '<<',  %>
<%#= link_to '<<'.html_safe, students_path(@students, @term), class: 'btn btn-default btn-lg' %>
<%#= link_to "<<", {:controller => "students", :action => "index", :student => @student}%>
    
  </div>
</div>
</div>






