
<%= form_for [@classroom, @term] do |f| %>
  <% if @term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@term.errors.count, "error") %> prohibited this term from being saved:</h2>

      <ul>
      <% @term.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
  
    Term <%= f.text_field :term, class: 'form-control', placeholder: 'Term' %>
  </div>
  
  

  <div class="actions">
    <%= f.submit "Submit", :class => 'btn btn-primary block' %>
  </div>
<% end %>



